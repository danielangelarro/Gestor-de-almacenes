   version: '3'
   services:
     db:
       image: postgres
       environment:
         POSTGRES_PASSWORD: admin
     backend:
       build: ./src/api
       depends_on:
         - db
     frontend:
       build: ./src/app
       depends_on:
         - backend